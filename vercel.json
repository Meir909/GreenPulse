{
  "buildCommand": "cd nextjs && npm run build",
  "outputDirectory": "nextjs/.next",
  "installCommand": "cd nextjs && npm install",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/:path*"
    }
  ]
}
